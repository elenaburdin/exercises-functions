function rectangleArea(width, height) {
    const rect = width * height;
    console.log(`The width ${width} and height ${height} of the rectangle area is ${rect}`);
}

rectangleArea(2, 2);
rectangleArea(10, 20);
rectangleArea(30, 30);
